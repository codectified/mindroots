LOAD CSV WITH HEADERS FROM 'file:///99names.csv' AS row
MERGE (arabic_root:Root {root: row.`الجذر`})

FOREACH(ignoreMe IN CASE WHEN row.`الجذر` IS NOT NULL AND row.`الجذر` <> '' THEN [1] ELSE [] END |
    MERGE (arabic_word:Word {script: 'arabic', text: row.`الجذر`})
    MERGE (arabic_root)-[:HAS_WORD]->(arabic_word)
)

FOREACH(ignoreMe IN CASE WHEN row.transliterated_root IS NOT NULL AND row.transliterated_root <> '' THEN [1] ELSE [] END |
    MERGE (transliterated_word:Word {script: 'transliteration', text: row.transliterated_root})
    MERGE (transliterated_root)-[:HAS_WORD]->(transliterated_word)
)

FOREACH(ignoreMe IN CASE WHEN row.`المصدر` IS NOT NULL AND row.`المصدر` <> '' THEN [1] ELSE [] END |
    MERGE (arabic_infinitive:Word {script: 'arabic', text: row.`المصدر`})
    MERGE (arabic_root)-[:HAS_INFINITIVE]->(arabic_infinitive)
)

FOREACH(ignoreMe IN CASE WHEN row.infinitive IS NOT NULL AND row.infinitive <> '' THEN [1] ELSE [] END |
    MERGE (english_infinitive:Word {script: 'english', text: row.infinitive})
    MERGE (transliterated_root)-[:HAS_INFINITIVE]->(english_infinitive)
)

FOREACH(ignoreMe IN CASE WHEN row.`اسم_الفاعل` IS NOT NULL AND row.`اسم_الفاعل` <> '' THEN [1] ELSE [] END |
    MERGE (arabic_active_participle:Word {script: 'arabic', text: row.`اسم_الفاعل`})
    MERGE (arabic_root)-[:HAS_ACTIVE_PARTICIPLE]->(arabic_active_participle)
)

FOREACH(ignoreMe IN CASE WHEN row.active_participle IS NOT NULL AND row.active_participle <> '' THEN [1] ELSE [] END |
    MERGE (english_active_participle:Word {script: 'english', text: row.active_participle})
    MERGE (transliterated_root)-[:HAS_ACTIVE_PARTICIPLE]->(english_active_participle)
)

FOREACH(ignoreMe IN CASE WHEN row.`اسم_المفعول` IS NOT NULL AND row.`اسم_المفعول` <> '' THEN [1] ELSE [] END |
    MERGE (arabic_passive_participle:Word {script: 'arabic', text: row.`اسم_المفعول`})
    MERGE (arabic_root)-[:HAS_PASSIVE_PARTICIPLE]->(arabic_passive_participle)
)

FOREACH(ignoreMe IN CASE WHEN row.passive_participle IS NOT NULL AND row.passive_participle <> '' THEN [1] ELSE [] END |
    MERGE (english_passive_participle:Word {script: 'english', text: row.passive_participle})
    MERGE (transliterated_root)-[:HAS_PASSIVE_PARTICIPLE]->(english_passive_participle)
)

FOREACH(ignoreMe IN CASE WHEN row.`اسم_المكان` IS NOT NULL AND row.`اسم_المكان` <> '' THEN [1] ELSE [] END |
    MERGE (arabic_noun_of_place:Word {script: 'arabic', text: row.`اسم_المكان`})
    MERGE (arabic_root)-[:HAS_NOUN_OF_PLACE]->(arabic_noun_of_place)
)

FOREACH(ignoreMe IN CASE WHEN row.noun_of_place IS NOT NULL AND row.noun_of_place <> '' THEN [1] ELSE [] END |
    MERGE (english_noun_of_place:Word {script: 'english', text: row.noun_of_place})
    MERGE (transliterated_root)-[:HAS_NOUN_OF_PLACE]->(english_noun_of_place)
)

FOREACH(ignoreMe IN CASE WHEN row.`اسم_المرة` IS NOT NULL AND row.`اسم_المرة` <> '' THEN [1] ELSE [] END |
    MERGE (arabic_singular:Word {script: 'arabic', text: row.`اسم_المرة`})
    MERGE (arabic_root)-[:HAS_SINGULAR]->(arabic_singular)
)

FOREACH(ignoreMe IN CASE WHEN row.singular IS NOT NULL AND row.singular <> '' THEN [1] ELSE [] END |
    MERGE (english_singular:Word {script: 'english', text: row.singular})
    MERGE (transliterated_root)-[:HAS_SINGULAR]->(english_singular)
)

FOREACH(ignoreMe IN CASE WHEN row.`اسم_الحال` IS NOT NULL AND row.`اسم_الحال` <> '' THEN [1] ELSE [] END |
    MERGE (arabic_noun_of_state:Word {script: 'arabic', text: row.`اسم_الحال`})
    MERGE (arabic_root)-[:HAS_NOUN_OF_STATE]->(arabic_noun_of_state)
)

FOREACH(ignoreMe IN CASE WHEN row.noun_of_state IS NOT NULL AND row.noun_of_state <> '' THEN [1] ELSE [] END |
    MERGE (english_noun_of_state:Word {script: 'english', text: row.noun_of_state})
    MERGE (transliterated_root)-[:HAS_NOUN_OF_STATE]->(english_noun_of_state)
)

FOREACH(ignoreMe IN CASE WHEN row.`اسم_الآلة` IS NOT NULL AND row.`اسم_الآلة` <> '' THEN [1] ELSE [] END |
    MERGE (arabic_noun_of_instrumentation:Word {script: 'arabic', text: row.`اسم_الآلة`})
    MERGE (arabic_root)-[:HAS_NOUN_OF_INSTRUMENTATION]->(arabic_noun_of_instrumentation)
)

FOREACH(ignoreMe IN CASE WHEN row.noun_of_instrumentation IS NOT NULL AND row.noun_of_instrumentation <> '' THEN [1] ELSE [] END |
    MERGE (english_noun_of_instrumentation:Word {script: 'english', text: row.noun_of_instrumentation})
    MERGE (transliterated_root)-[:HAS_NOUN_OF_INSTRUMENTATION]->(english_noun_of_instrumentation)
)

FOREACH(ignoreMe IN CASE WHEN row.`اسم_ذات` IS NOT NULL AND row.`اسم_ذات` <> '' THEN [1] ELSE [] END |
    MERGE (arabic_noun_of_essence:Word {script: 'arabic', text: row.`اسم_ذات`})
    MERGE (arabic_root)-[:HAS_NOUN_OF_ESSENCE]->(arabic_noun_of_essence)
)

FOREACH(ignoreMe IN CASE WHEN row.noun_of_essence IS NOT NULL AND row.noun_of_essence <> '' THEN [1] ELSE [] END |
    MERGE (english_noun_of_essence:Word {script: 'english', text: row.noun_of_essence})
    MERGE (transliterated_root)-[:HAS_NOUN_OF_ESSENCE]->(english_noun_of_essence)
)

FOREACH(ignoreMe IN CASE WHEN row.`اسم_التفعيل` IS NOT NULL AND row.`اسم_التفعيل` <> '' THEN [1] ELSE [] END |
    MERGE (arabic_noun_of_hyperbole:Word {script: 'arabic', text: row.`اسم_التفعيل`})
    MERGE (arabic_root)-[:HAS_NOUN_OF_HYPERBOLE]->(arabic_noun_of_hyperbole)
)

FOREACH(ignoreMe IN CASE WHEN row.noun_of_hyperbole IS NOT NULL AND row.noun_of_hyperbole <> '' THEN [1] ELSE [] END |
    MERGE (english_noun_of_hyperbole:Word {script: 'english', text: row.noun_of_hyperbole})
    MERGE (transliterated_root)-[:HAS_NOUN_OF_HYPERBOLE]->(english_noun_of_hyperbole)
)
